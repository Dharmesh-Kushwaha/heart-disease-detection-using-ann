# heart-disease-detection-using-ann
A Deep Learning based Heart Disease Prediction System using Artificial Neural Network (ANN).  The model is trained on a health dataset and deployed as an interactive web application using Streamlit.  It includes data preprocessing, feature scaling, ANN model training, and real-time prediction. 
